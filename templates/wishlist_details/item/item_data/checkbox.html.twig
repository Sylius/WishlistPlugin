{% set product = hookable_metadata.context.product %}
{% set itemForm = hookable_metadata.context.itemForm %}
{% set index = hookable_metadata.context.index %}

<div class="bb-wishlist-item-select">
    <input type="checkbox"
           id="wishlist_item_selected_{{ index }}"
           data-model="selection[{{ index }}]"
    />
    </div>
